import { ComponentHarness } from '@angular/cdk/testing';

/**
 * Implement this class for NxFormFieldHarness.getControl() to accept a specific Control Harness
 */
export class NxFormFieldControlHarness extends ComponentHarness {
  // abstract isValid(): Promise<boolean>;
  // abstract isInvalid(): Promise<boolean>;
  // abstract getValue<T extends string>(): Promise<T>;
  // abstract setValue<T extends string>(value: T): Promise<void>;
}
